// http://codeforces.com/problemset/problem/955/C

// total queries = 0
// for k in range(L, R + 1):
//  find all prime factors
//  partition the factors into all possible 2 groups, where one group > 0 and the other one > 1
//  add (# of groups / 2) to the total queries
// return the total queries